{
  "name": "zitlas-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Firebase Emulator security-rules tests for ZITLAS firestore.rules",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project zitlas-b8677 \"mocha rules.test.js --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^15.24.0",
    "mocha": "^10.4.0"
  }
}
