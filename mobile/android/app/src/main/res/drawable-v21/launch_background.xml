<?xml version="1.0" encoding="utf-8"?>
<!-- API 21+ variant. Was ?android:colorBackground (the OS theme's background,
     which is white under the Light launch theme). Pinned to the ZITLAS splash
     background so the pre-Flutter frame matches the splash exactly — see the
     non-v21 copy of this file for the full reasoning. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/zitlas_splash_bg" />
</layer-list>
